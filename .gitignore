# ── Environment ──────────────────────────────────────
.env
!.env.example

# ── Python ───────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Muzey kontent ma'lumotlari (gitda saqlanadi) ──────
# bolimlar.json, vitrinas.json, eksponatlar.json — gitga kiradi
# Lokal qo'shilgan ma'lumot push qilinganda serverga ham boradi

# ── Foydalanuvchi statistikasi (gitda saqlanmaydi) ────
# Serverda to'planadi, har deploy da o'chirilmasin
backend/data/likes.json
backend/data/visits.json
backend/audio_cache.json

# ── User uploads (binary fayllar) ────────────────────
backend/uploads/*
!backend/uploads/.gitkeep

# ── OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db
